# border-shape: snake

A single-div snake game (and it's not canvas!).

Snake is border-shape, everything else is pseudo.

Learn about [`border-shape`](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/border-shape).

Only works with browsers supporting border-shape